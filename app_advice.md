#PWAApp修改建议文档：面向普通用户的智能插排移动端优化方案

##一、修改目标

当前PWAApp已经具备设备查看、插孔开关、功率曲线、告警、本地缓存、邮件提醒等基础功能，但整体仍偏“工程调试工具”，对于普通用户来说存在以下问题：

1.首页信息不够聚焦，用户打开App后不能快速知道“现在是否安全、是否需要处理”。
2.插排控制功能较多，但缺少分层，普通开关、节能、睡眠、紧急断电、学习纠错等操作都堆在一起，容易造成理解负担。
3.告警页偏本地告警列表，没有充分接入后端行为事件和端侧行为标签。
4.设备Unknown、待机浪费、protected_cutoff等核心行为分析结果没有被转化成用户容易理解的解释。
5.网页端已有较强的行为分析和AI能力，但App尚未形成适合移动端的轻量化表达。

因此，App优化目标不是把网页控制台完整复制到手机端，而是将App定位为：

> 面向学生或普通使用者的移动端用电助手，重点完成“查看状态、控制插孔、理解提醒、处理异常、优化习惯”五件事。

最终希望用户打开App后可以快速完成：

```text
知道当前插排是否正常；
知道哪个插孔需要关注；
可以安全地开关插孔；
可以理解为什么被提醒或断电；
可以提交设备类型帮助系统学习；
可以开启适合自己的提醒方式。
```

---

##二、App与网页端的功能边界

###2.1App应该承担的功能

App适合承担高频、个人化、轻量化的操作：

| 功能类型 | App职责                                |
| ---- | ------------------------------------ |
| 日常查看 | 查看当前功率、插孔状态、设备在线状态                   |
| 日常控制 | 单插孔开关、全部关闭、睡眠模式、一键节能                 |
| 异常理解 | 解释Unknown、待机浪费、protected_cutoff、设备离线 |
| 简单处理 | 忽略提醒、重试控制、提交设备类型、开启个人邮件提醒            |
| 轻量分析 | 今日用电体检、待机提示、最近异常、个人用电建议              |
| 安全确认 | 对恢复供电、紧急断电等操作进行二次确认                  |

###2.2App不应该承担的功能

以下功能不建议放入学生端App，或只对管理员角色开放：

| 功能          | 原因              |
| ----------- | --------------- |
| 学生账号管理      | 属于网页管理端能力       |
| 全局房间态势      | 移动端展示复杂，普通用户不需要 |
| 楼层拓扑        | 适合大屏或网页端        |
| SMTP邮件总配置   | 属于管理员系统配置       |
| 房间级策略复杂配置   | 普通用户容易误操作       |
| CSV导出       | 移动端使用频率低        |
| 全局AI开放式问答   | 容易复杂化，建议只保留轻量问答 |
| 批量修改多个宿舍策略  | 属于管理端权限         |
| 高风险自动断电策略配置 | 必须由管理员在网页端配置    |

App应保持克制，不要堆叠过多管理功能。它的价值在于让每个普通用户都能方便、合理、安全地控制自己的设备。

---

##三、整体信息架构调整

当前App底部有4个Tab：

```text
概览
插排
告警
我的
```

这个结构可以保留，但每个Tab的重点需要重新定义。

建议改为：

```text
首页
插孔
提醒
我的
```

如果希望保留原名，也可以继续使用“概览/插排/告警/我的”，但页面内容按下面方案调整。

---

##四、首页改造建议

###4.1首页定位

首页不应只是功率和曲线展示，而应成为用户打开App后的“用电状态总览”。

用户打开首页后应立刻看到：

1.当前是否正常；
2.当前总功率；
3.有没有需要处理的提醒；
4.哪个插孔最值得关注；
5.今天的用电建议。

###4.2首页推荐结构

首页从上到下建议为：

```text
顶部状态卡
↓
今日用电体检卡
↓
当前插孔摘要
↓
功率趋势小图
↓
最近提醒
↓
快捷操作
```

###4.3顶部状态卡

显示：

* 当前设备名称；
* 在线/离线状态；
* 当前总功率；
* 今日估算用电；
* 当前风险等级。

示例：

```text
A303智能插排
在线
当前功率：86W
今日用电：0.42kWh
当前状态：正常
```

如果异常：

```text
当前状态：需要关注
插孔3出现未知高功率负载
```

###4.4今日用电体检卡

建议新增一个“今日用电体检”模块，这是App最有价值的行为分析入口。

卡片展示：

* 今日总体评价；
* 今日最高功率；
* 今日待机时长；
* 今日异常次数；
* 一句话建议。

示例：

```text
今日用电体检
总体：基本正常
最高功率：420W
待机提醒：插孔2待机3.5小时
异常提醒：1次Unknown设备提醒

建议：睡前可以关闭插孔2，减少待机耗电。
```

接口建议：

```text
GET/api/rooms/{room_id}/daily-checkup?device={deviceId}
```

如果后端接口暂时没有完成，可以先用当前`status+telemetry+behavior`在前端做简化展示，但最终应以后端统计结果为准。

###4.5当前插孔摘要

首页不必展示完整插孔控制细节，只显示4个小卡片：

```text
插孔1：台灯 12W 正常
插孔2：充电器 3W 待机
插孔3：Unknown 420W 需关注
插孔4：关闭
```

点击后进入“插孔”页对应卡片。

###4.6功率趋势小图

首页保留轻量功率曲线，但不要占据过多空间。默认展示最近1小时即可。

如果用户想看更长周期，可在卡片上提供：

```text
查看24小时
查看7天
```

但不要在首页堆叠太多历史分析。

###4.7最近提醒

首页只展示最近1~3条重要提醒：

```text
22:41 插孔3出现Unknown高功率负载
00:12 插孔2低功率待机超过3小时
```

点击进入“提醒”页详情。

---

##五、插孔页改造建议

###5.1插孔页定位

插孔页是App的核心控制页。它必须满足：

1.用户可以快速控制每个插孔；
2.用户可以看懂每个插孔当前状态；
3.高风险状态下不能误操作；
4.Unknown和protected_cutoff要有专门处理流程。

###5.2插孔卡片内容

每个插孔卡片建议展示：

```text
插孔编号
开关状态
当前功率
识别设备
行为状态
风险等级
主要原因
操作按钮
```

示例：

```text
插孔3
Unknown设备
420W
状态：需关注
原因：设备未识别，且功率较高
按钮：查看原因  关闭插孔
```

正常状态：

```text
插孔1
Desk_Lamp
12W
状态：正常
按钮：关闭
```

待机状态：

```text
插孔2
Phone_Charger
3W
状态：待机较久
原因：低功率持续3.5小时
按钮：关闭  不再提醒
```

protected_cutoff状态：

```text
插孔3
已保护断电
状态：已断电保护
原因：Unknown高功率持续运行
按钮：查看原因
```

此时不要直接显示普通“开启”按钮。

###5.3插孔操作分层

当前App已有全部开启、全部关闭、一键节能、睡眠模式、紧急断电、重识别、学习提交等功能。建议按照操作风险分层展示。

####普通操作

直接展示：

* 单插孔开关；
* 全部关闭；
* 睡眠模式；
* 一键节能。

####中风险操作

需要确认弹窗：

* 全部开启；
* 关闭当前高功率插孔；
* 恢复刚被关闭的插孔。

####高风险操作

需要强确认，必要时仅管理员可用：

* 紧急断电；
* protected_cutoff后恢复供电；
* 对Unknown高功率插孔重新开启；
* 对多个插孔批量开启。

###5.4建议保留的快捷操作

移动端不要堆太多按钮。推荐只保留4个快捷操作：

```text
全部关闭
睡眠模式
一键节能
紧急断电
```

其中：

* “全部关闭”适合离开宿舍；
* “睡眠模式”适合晚上保留必要设备，关闭非必要插孔；
* “一键节能”适合关闭待机浪费插孔；
* “紧急断电”需要红色按钮和二次确认。

不建议在首页或插孔页显眼位置放“全部开启”，它更容易造成误操作。可以放到更多菜单里，并要求确认。

###5.5Unknown设备处理流程

当某个插孔识别为Unknown时，不应只显示Unknown，而应引导用户处理。

流程：

```text
显示Unknown提醒
↓
解释为什么Unknown
↓
显示功率范围和当前功率
↓
让用户选择设备类型
↓
提交学习
↓
提示“已提交，系统后续会减少误提醒”
```

界面示例：

```text
系统暂时无法识别插孔3的设备
当前功率：420W
建议确认设备类型

请选择设备类型：
手机充电器
笔记本
台灯
风扇
电热水壶
电吹风
其他
```

注意：不要直接写“违规设备”，应写“未知设备”或“需要确认设备类型”。

###5.6protected_cutoff处理流程

protected_cutoff是App必须重点优化的状态。

当插孔处于protected_cutoff时：

1.卡片显示“已保护断电”；
2.隐藏普通开关；
3.显示“查看原因”按钮；
4.原因页解释触发条件；
5.如果学生端权限不足，显示“请联系管理员”；
6.如果允许用户恢复，必须强确认。

学生端默认建议：

```text
该插孔已进入保护断电状态。请先确认接入设备是否正常，必要时联系管理员处理。
```

不建议直接提供：

```text
一键恢复供电
```

除非后端权限明确允许。

---

##六、提醒页改造建议

###6.1提醒页定位

当前告警页主要是本地告警，建议升级为“提醒中心”，统一展示：

* 设备离线；
* 控制失败；
* 待机浪费；
* Unknown高功率；
* protected_cutoff；
* 频繁开关；
* 系统异常。

###6.2提醒分类

建议按用户理解方式分类，而不是按技术标签分类：

```text
全部
需要处理
用电提醒
设备异常
控制记录
```

其中：

| 分类   | 包含内容                             |
| ---- | -------------------------------- |
| 需要处理 | protected_cutoff、Unknown高功率、控制失败 |
| 用电提醒 | 待机浪费、长时间高负载                      |
| 设备异常 | 离线、重连失败                          |
| 控制记录 | 开关成功、模式切换、命令失败                   |

###6.3提醒卡片内容

每条提醒卡片建议展示：

```text
标题
发生时间
涉及插孔
简短原因
风险等级
处理按钮
```

示例：

```text
Unknown高功率提醒
插孔3 · 22:41
系统暂时无法识别该设备，且功率达到420W
按钮：查看原因  标注设备  关闭插孔
```

待机提醒：

```text
待机时间较长
插孔2 · 已持续3.5小时
该设备处于低功率运行状态，可能存在待机耗电
按钮：关闭插孔  不再提醒
```

离线提醒：

```text
设备离线
A303智能插排 · 5分钟前
暂时无法获取设备状态，0W不代表设备已关闭
按钮：重试连接
```

###6.4接入后端行为事件

建议提醒页不要只依赖本地alerts，应接入：

```text
GET/api/behavior-events?device={deviceId}&limit=50
```

同时保留本地告警作为补充。最终展示逻辑：

```text
后端行为事件为主；
本地控制失败和网络异常为辅。
```

---

##七、我的页改造建议

###7.1我的页定位

我的页不应承载太多设备控制功能，主要负责：

* 用户信息；
* 设备绑定；
* 提醒偏好；
* 网络连接设置；
* App设置；
* 帮助与反馈。

###7.2建议保留内容

保留：

* 用户名；
* 邮箱；
* 当前绑定设备；
* 邮件提醒开关；
* 最近7天提醒次数；
* API/WS调试设置；
* 健康检查；
* 退出登录；
* 重置App。

###7.3新增“提醒偏好”

普通用户最需要的是“怎么提醒我”，而不是复杂策略配置。

建议新增：

```text
提醒偏好
- 邮件提醒
- App内提醒
- 待机提醒
- Unknown设备提醒
- 设备离线提醒
```

注意：这里是个人提醒偏好，不是管理员策略。它不应直接修改房间级自动断电策略。

###7.4新增“帮助中心”

帮助中心放常见问题：

```text
为什么显示Unknown？
为什么插座被保护断电？
设备离线怎么办？
怎么减少待机耗电？
为什么控制失败？
```

可以复用知识库内容，但移动端展示应更短。

---

##八、轻量AI助手建议

###8.1是否接入AI助手

建议接入，但不要照搬网页端完整AI助手。

网页端AI适合管理员做：

```text
全局分析
策略建议
巡检报告
学生管理辅助
复杂查询
```

App端AI应该是：

```text
固定问题优先
轻量解释为主
少量可确认动作
不做复杂管理
```

###8.2入口设计

建议不在底部增加第5个Tab，而是在首页和提醒详情页放轻量入口：

```text
问问助手
为什么提醒我？
怎么处理？
```

或者在右下角放小型悬浮按钮，但默认只展开快捷问题。

###8.3App端推荐问题

固定问题建议：

```text
今天用电正常吗？
为什么提醒我？
为什么显示Unknown？
为什么插座被保护断电？
哪些插孔可以关闭省电？
设备离线怎么办？
我该如何减少待机耗电？
```

###8.4AI回复形式

App端AI回复不要长篇大论，建议采用：

```text
一句话结论
↓
2~3条证据
↓
1~2个建议
↓
必要时显示操作按钮
```

示例：

```text
插孔3被提醒，是因为系统暂时无法识别设备，且当前功率较高。

证据：
1.设备类型：Unknown
2.当前功率：420W
3.持续时间：6分钟

建议：
先确认接入设备类型。如果是正常设备，可以提交设备类型，帮助系统学习。
```

---

##九、App控制逻辑优化

###9.1控制动作必须有状态反馈

所有插孔控制都应保持：

```text
点击按钮
↓
按钮进入执行中
↓
等待WebSocket ACK
↓
如果没有ACK，轮询cmd状态
↓
显示成功或失败
```

这一点当前App已经有基础能力，应继续保留。

###9.2避免连续误触

建议增加：

* 同一插孔命令执行中禁止重复点击；
* 全部关闭、睡眠模式、紧急断电必须弹窗确认；
* 高功率插孔关闭前提示当前功率；
* protected_cutoff恢复供电必须强确认；
* 离线设备禁止显示可执行控制按钮，只显示“设备离线，无法控制”。

###9.3按钮文案更贴近用户

不建议只显示技术按钮：

```text
correct
learn_commit
protected_cutoff
```

应改为用户语言：

| 技术含义               | App显示   |
| ------------------ | ------- |
| learn_commit       | 提交设备类型  |
| correct            | 重新识别    |
| protected_cutoff   | 已保护断电   |
| standby_waste      | 待机较久    |
| unknown_high_power | 未知高功率设备 |
| long_high_power    | 长时间高负载  |
| frequent_switching | 开关频繁    |

---

##十、移动端页面细节建议

###10.1底部Tab

建议为：

```text
首页
插孔
提醒
我的
```

图标建议：

* 首页：房子/仪表盘；
* 插孔：插头；
* 提醒：铃铛；
* 我的：用户。

###10.2颜色层级

建议统一风险颜色：

```text
正常：绿色
提醒：蓝色或灰色
中风险：橙色
高风险：红色
离线：灰色
执行中：蓝色
```

不要让所有提醒都红色，否则用户容易疲劳。

###10.3卡片层级

移动端优先卡片，不用复杂表格。

推荐卡片类型：

* 状态卡；
* 插孔卡；
* 提醒卡；
* 建议卡；
* 原因解释卡；
* 操作确认卡。

###10.4减少复杂图表

App中图表只保留：

* 最近1小时功率；
* 今日功率简图；
* 最近7天用电趋势小图。

复杂的历史账单、CSV、红黑榜、楼层拓扑留在网页端。

---

##十一、推荐新增接口

###11.1今日用电体检

```text
GET/api/rooms/{room_id}/daily-checkup?device={deviceId}
```

用于首页今日体检卡。

返回：

```json
{
  "status":"normal",
  "riskLevel":"low",
  "summary":"今日用电基本正常",
  "totalEnergyKwh":0.42,
  "peakPowerW":420,
  "standbyHours":3.5,
  "alertCount":1,
  "topFocusSocket":2,
  "suggestions":[
    "插孔2待机时间较长，睡前可关闭"
  ]
}
```

###11.2App版行为摘要

```text
GET/api/devices/{deviceId}/mobile-summary
```

用于App首页和插孔页快速展示。

返回：

```json
{
  "deviceId":"strip01",
  "online":true,
  "totalPowerW":86,
  "riskLevel":"medium",
  "focus":{
    "socketId":3,
    "title":"未知高功率设备",
    "message":"插孔3功率较高且设备未识别"
  },
  "sockets":[
    {
      "id":1,
      "on":true,
      "powerW":12,
      "device":"Desk_Lamp",
      "behaviorTag":"normal",
      "riskLevel":"low"
    }
  ]
}
```

###11.3行为事件列表

```text
GET/api/behavior-events?device={deviceId}&limit=50
```

用于提醒页。

###11.4待机分析

```text
GET/api/devices/{deviceId}/standby-summary?period=7d
```

用于首页和AI快捷问题。

###11.5Unknown设备摘要

```text
GET/api/devices/{deviceId}/unknown-summary?period=7d
```

用于Unknown处理页。

---

##十二、优先级建议

###第一阶段：先让App更像日常使用工具

优先完成：

1.首页改为“状态+今日体检+最近提醒”；
2.插孔卡片增加行为标签、风险等级、原因说明；
3.protected_cutoff专门状态展示；
4.Unknown设备处理流程；
5.提醒页接入后端行为事件；
6.控制按钮二次确认优化。

###第二阶段：补齐轻量行为分析

继续完成：

1.今日用电体检接口接入；
2.待机浪费分析卡；
3.最近7天用电简报；
4.个人提醒偏好；
5.帮助中心。

###第三阶段：接入轻量AI助手

最后完成：

1.固定快捷问题；
2.解释Unknown、待机、断电、离线；
3.根据当前页面上下文回答；
4.支持少量动作卡片；
5.学生端友好话术。

---

##十三、推荐最终App功能清单

最终App建议保留以下功能即可，不要再继续堆管理功能。

###首页

* 当前设备状态；
* 当前总功率；
* 今日用电体检；
* 当前最值得关注插孔；
* 最近功率曲线；
* 最近提醒；
* 快捷进入插孔控制。

###插孔

* 每路插孔状态；
* 单插孔开关；
* 当前功率；
* 设备类型；
* 行为标签；
* 风险等级；
* Unknown设备标注；
* protected_cutoff查看原因；
* 全部关闭；
* 睡眠模式；
* 一键节能；
* 紧急断电。

###提醒

* 行为事件；
* 控制失败；
* 设备离线；
* 待机提醒；
* Unknown提醒；
* protected_cutoff提醒；
* 查看原因；
* 忽略提醒；
* 重试操作。

###我的

* 用户信息；
* 绑定设备；
* 邮件提醒；
* 个人提醒偏好；
* 帮助中心；
* 连接设置；
* 健康检查；
* 退出登录；
* 调试模式。

###轻量助手

* 今天用电正常吗；
* 为什么提醒我；
* 为什么显示Unknown；
* 为什么被保护断电；
* 哪些插孔可以省电；
* 设备离线怎么办。

---

##十四、答辩或展示表述

可以这样描述App端设计：

> 网页控制台主要面向管理员，提供设备管理、策略配置、行为分析、学生管理和AI助手等完整能力；移动端PWA则面向学生和普通使用者，强调日常查看和安全控制。App不堆叠复杂管理功能，而是围绕“状态查看、插孔控制、提醒处理、设备学习、个人提醒”五个高频场景设计。用户打开App后可以快速看到当前插排是否正常、哪个插孔需要关注，并通过清晰的操作确认机制安全控制设备。当出现Unknown设备、待机浪费或保护断电时，App会给出面向普通用户的解释和处理建议，从而让用电行为分析真正落地到日常使用中。
